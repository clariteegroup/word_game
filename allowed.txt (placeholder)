# allowed.txt will be generated by scripts/generate_answers.py
# This placeholder file is committed so the repo tracks the path.
# The CI workflow or the script will overwrite this with the merged allowed-guesses list
# (answers union any existing allowed words), one lowercase word per line.
